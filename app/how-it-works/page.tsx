import Link from 'next/link'
import { ArrowLeft, ArrowRight, Check } from 'lucide-react'

const steps = ['Capture the source', 'Bind the identity', 'Verify the moment']

export default function HowItWorksPage() {
  return <main className="mx-auto max-w-7xl px-5 py-24 sm:px-8 lg:py-40"><Link href="/" className="inline-flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.22em] text-muted-foreground hover:text-primary"><ArrowLeft className="h-4 w-4" /> Back to signal</Link><div className="mt-20 max-w-4xl"><p className="eyebrow text-primary">VIGIL / Protocol</p><h1 className="display-title mt-6">The anatomy of<br /><span className="text-primary">a trusted voice.</span></h1><p className="mt-10 max-w-xl text-lg leading-relaxed text-muted-foreground">VIGIL works at the source, not after the fact. Every recording carries an integrity layer that can be checked without revealing private identity.</p></div><div className="mt-24 border-t border-border">{steps.map((step, index) => <article key={step} className="grid gap-6 border-b border-border py-10 md:grid-cols-[100px_1fr_1fr]"><span className="text-sm text-primary">0{index + 1}</span><h2 className="text-3xl tracking-tight">{step}</h2><p className="max-w-md leading-relaxed text-muted-foreground">{index === 0 ? 'A local acoustic fingerprint is taken before audio leaves its source. Nothing is uploaded until the signal is ready.' : index === 1 ? 'The fingerprint is sealed to a private source identity using a tamper-evident signature.' : 'A verifier reads the seal and returns an understandable result: authentic, altered, or unknown.'}</p></article>)}</div><div className="mt-20 flex flex-wrap gap-6 text-xs uppercase tracking-[.2em]"><span className="inline-flex items-center gap-2 text-primary"><Check className="h-4 w-4" /> Private by design</span><span className="inline-flex items-center gap-2 text-primary"><Check className="h-4 w-4" /> Open to verify</span><Link href="/about" className="inline-flex items-center gap-2 hover:text-primary">About VIGIL <ArrowRight className="h-4 w-4" /></Link></div></main>
}
